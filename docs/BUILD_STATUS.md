# Build status — 2026-09-19

## Public read-only demo — verified

User requests public access at banyan.aaravreddy.org, no visitor edits, and the three existing demo credentials on sign-in; all other behavior and appearance stay unchanged. Preflight: clean `aarav`, matching `origin/aarav` after fetch. Coordinator owns sign-in presentation/tests, runtime activation, documentation, integration and Git. Backend engineer owns only `apps/api/config/settings.py`, `apps/api/config/middleware.py`, `apps/api/philanthra/accounts/views.py` and a focused `tests/test_public_demo.py`; no migrations or dependency changes. Security reviewer is read-only across request routes and authentication. Frozen interfaces: existing endpoints and permissions remain unchanged when public read-only mode is off; session metadata may gain an additive `demo_read_only` boolean. Public mode must allow session login/logout and reads, reject all visitor mutations server-side, retain CSRF, and never reveal real account secrets. Acceptance: adversarial API mutation tests, credential visibility tests, full `make verify`, and live-domain login/read/write-denial checks. No Git operations or recursive delegation by subagents.

Backend ownership extension: only `SubscriptionsView.get` in `apps/api/philanthra/core/views.py` and `ProfileView.get` in `apps/api/philanthra/pilot/views.py` may also change, to return existing/default records without creating rows in public read-only mode. Coordinator additionally owns the session schema and generated API contract, preserving the additive boolean interface.

Completed: opt-in `PHILANTHRA_DEMO_READ_ONLY=1` (demo environment only) denies visitor writes before API dispatch, including password reset, invitations, role changes, uploads and reviews, while retaining CSRF-protected login/logout and reads. No account/host/header bypass exists. The two GET defaults no longer create domain records in this mode. Sign-in displays the three existing demo credentials only in demo mode; the existing banner identifies read-only mode. Other UI, workflows when the flag is off, dependencies, migrations and data remain unchanged. Native backend/security agents returned ownership; the independent reviewer found no remaining concrete visitor mutation path. Session bookkeeping and access audits remain enabled, and pre-existing background jobs can run independently.

Verification: `make verify` passed exit0: Ruff/format/mypy, Django/migration checks, TypeScript and API contract drift, **206 backend tests plus 1,361 subtests, 26 component tests, 24 desktop/mobile browser journeys**, production build and real-backend smoke. Focused API tests enumerate the actual URL tree for anonymous visitors, the three published roles and platform-admin, with unchanged content/user snapshots, CSRF and normal-mode regressions. Initial sandbox-only DB test setup failed on loopback permissions; the full permitted gate executed successfully. Historical screenshots regenerated by E2E were restored byte-for-byte.

Live: the existing Banyan Site/domain and proxy deployment were preserved. The local app was restarted with the private tunnel settings and read-only mode; no migration, reset or reseed was performed. After checking the local read-only flag, only the existing ngrok viewer policy was removed. `https://banyan.aaravreddy.org` passed unauthenticated access, all three CSRF logins, reads, six write-denial checks per account and logout. Live Chromium at 1440px/390px passed visible three-row credentials, no horizontal overflow, role login, read-only banner and no page errors; mobile screenshot was visually inspected. Ignored runtime configuration, process records, logs and screenshots are not committed. Same-host restart helper: `python3 .runtime/start-public-demo.py` after stopping app services; it preserves the private settings and does not reseed. Git commit/push is the coordinator's final step after this record. Mac/worker/ngrok uptime remains required; this is a public synthetic read-only demo, not a production-data deployment.

## Deployment preflight — altrive.aaravreddy.org

Requested deployment preserves existing features. Coordinator owns deployment configuration, this status record and Git; native `release_engineer` performed a bounded read-only runtime/deployment audit and returned ownership with no edits. Application source and interfaces remain unchanged. Preflight started at `171735a`, matching `origin/aarav` after fetch; unrelated untracked `Archive.zip` is preserved.

Observed: `make doctor` passed; Docker remains unavailable. DNS lookup returned GoDaddy nameservers for `aaravreddy.org` and no A record for `altrive.aaravreddy.org`. No applicable hosting/DNS credentials were found in the project or usual local CLI configuration locations. Existing Chrome access failed because Computer Use permissions were not granted. The user was asked for the credential location or hosting account, not a password.

No deployment or DNS changes were made. Full functionality requires Next.js, Django/Gunicorn, PostgreSQL and the background worker. Existing Compose/startup is local-demo configuration; production rejects seeded demo identities. Hosting setup must provide trusted HTTPS forwarding, dedicated secrets/storage and initial account provisioning; email delivery requires configuration. A hosted copy of the seeded experience needs an isolated, protected demo configuration. No production prerequisite, container verification or remote runtime success is claimed. Next action: obtain existing hosting access, configure the chosen runtime without changing features, then verify HTTPS, authentication, persistence and worker-backed journeys.

## Repository and milestones

The runnable pilot is implemented on `aarav`. No main changes, force push, invented remote, payment workflow or public deployment occurred. The original proposal is archived byte-for-byte (SHA-256 `d599bff2bdba60792f1ec7024835ede99071db77e6c5a9cba0c48cb521e8a0a4`), along with original Git instructions and the complete build request. Foundation commit `00db4c3` and integrated application commit `4f50a0e` were pushed to origin/aarav. The final release commit is the commit containing this checkpoint; consult `git log -1` for its ID.

| Milestone | Delivered behavior |
| --- | --- |
| M0 | Preflight, archived inputs, native specialist reviews, TOML role definitions, architecture/contracts, normalized schema, auth/health, environment and test harness. |
| M1 | Backend-seeded directory, fixture financial ingestion/signals, discovery/detail/comparison and exact-cent persistent allocation with provenance and isolation. |
| M2 | Mapped aggregate upload, leased worker validation/commit, program/card editing, independent review, purpose-specific sharing, matching/alerts, withdrawal and deletion. |
| M3 | Fixed pooled binary analysis/suppression, graph, source-bound reports, watchlists/periodic monitoring, current geographic coverage, benchmarks/opportunities, identity/contact/onboarding and opt-in metrics. |
| M4 | Inspected IRS/status/ACS/IATI local adapters, bounded opt-in source fetching, manual catalogue import, deterministic explanations, optional grounded provider and honest evaluation harness. |
| M5 | Full interface, desktop/mobile persisted journeys, independent security/race regressions, production guards, setup/restore/restart checks, source-linked docs and handoff. Compose verification remains external. |

## Observed final checks

Native environment: macOS ARM64; PostgreSQL 17.11; project Python 3.12.4; Node 24.14.0; pinned uv 0.12.17 / pnpm 12.4.2. Lockfiles resolve Django 5.2.17, Next 16.3.5 and React 19.3.0.

| Command / check | Observed result |
| --- | --- |
| `make doctor` | Passed; Docker absent, native PostgreSQL detected. |
| `make setup` | Passed using frozen lockfiles and registry access; 68 Python packages checked, JavaScript lock unchanged. Initial sandbox DNS failure was not treated as success. No global conda modification. |
| `make verify` | Passed, exit 0: **195 backend tests in 49.72 seconds, 14 component tests, 14 browser tests**; CI follow-up recorded below, production build, checks and real-backend smoke. |
| `make check` (within verify) | Ruff lint/format, domain/parser mypy (14 files), Django system/migration drift, Prettier, strict TypeScript and generated OpenAPI/client drift passed. |
| `make build` | Production Next build passed. |
| Real-backend browser tests | Passed all fourteen cases across 1440px desktop and 390px mobile; main contexts block external destinations and use real local API responses. |
| `make smoke` | Passed fresh migrations/seed, readiness, session/CSRF login and persisted discovery. |
| `make demo`, repeated `make stop`/`make demo` | Passed actual loopback readiness. Rapid restart regression fixed; **183 records across seven domain tables** retained matching full-row hashes. Running URL: http://127.0.0.1:8080/demo. |
| `make restore-test` | Passed isolated scratch restore: **49 tables / 1,244 rows**, full-row SHA-256 hashes from the same PostgreSQL snapshot. Scratch database removed. This does not certify future backups. |
| Dependency audits | Earlier Python and JavaScript network advisory audits reported no known vulnerabilities for these lockfiles. No dependency changes followed. Network audits are separate from the deterministic gate. |
| Optional live provider smoke | Explicitly running without its opt-in flag produced **1 skipped**, no network. Not part of default test collection; no live model test success is claimed. |
| Original archive and diff | Original README blob equals archived proposal byte-for-byte; `git diff --check` passed. |

## Coverage and what it proves

Backend coverage includes branch measurement: approximately **81.7% line coverage and 65.8% branch coverage** (combined pytest-cov display 78%). Allocation has **100% line and branch coverage**; pooling 97.4% lines / 95.2% branches; policy 80.4% / 71.9%; core transactional services 90.1% / 72.6%; worker 83.9% / 66.3%. These are observed module coverage, not claims of exhaustive security. API orchestration branches, operator CLI error variants, graph variants and rare worker recovery paths have lower coverage. Browser execution is separate and is not merged into Python coverage. See generated local `coverage.xml` for exact statement/branch counts: 3,783 / 4,633 lines and 943 / 1,434 branches.

Tests use hand-checkable financial/pooling examples, Hypothesis allocation properties and real PostgreSQL concurrent transactions. Independent regressions verify queued edits, publication and workers waiting behind withdrawal locks, stale lease fencing, reviewer authority, private existence/count/citation/export isolation and identity invalidation. Default tests never call live external data/model services.

## Inspection, findings and ownership

Actual desktop/mobile screenshots were captured and opened using image tools. Discovery passed configured axe WCAG A/AA checks and keyboard/overflow/console assertions at both widths. This is not application-wide accessibility certification. Representative review, analysis, portfolio and NGO views were inspected; screenshots prove rendering, not backend/scientific correctness.

Security findings and focused rechecks are in SECURITY_REVIEW.md; independent journey evidence is in QA_REPORT.md. Fixed integration defects include reviewer privilege escalation, authorization after a blocked edit, stale identity linkage, production host defaults, opportunity lookup, forgotten attribution, source/target alert invalidation, hidden match titles, missing-numerator collection prompts, contrast/SVG semantics, synthetic service-area omission and TCP TIME_WAIT restart handling. Assertions were retained when fixing defects.

Native GPT-6 Astra/high agents performed product, security, data, analytics, backend, frontend, QA and release responsibilities through bounded assignments. Custom TOML definitions do not reload this session; equivalent explicit native role briefs were used. All specialist threads completed and handed ownership back before Git operations. No subagent spawned another agent or mutated Git. Coordinator owns the final integrated files, manifests/contracts, documentation and Git.

## Acceptance boundaries and next operator actions

R01–R24 are mapped to implementation and executable checks in ACCEPTANCE_MATRIX.md. The final independent read-only audit found and rechecked the workspace-creation UI and portfolio geography additions; no material concern remained from that bounded review.

- **Docker/Compose has not been executed here** because Docker is unavailable. Linux/fresh clone on another machine, production TLS/proxy, real email, encrypted production storage/backups, operational load and remote CI are not claimed verified. Run the documented Compose/fresh-clone gate before deployment.
- Fixtures cover inspected minimal IRS variants, not every authentic filing. IATI/status adapters preserve source-reported registry structures; they do not manufacture validated program outcomes. Public community context is a narrow city QuickFacts extract, not ZIP-level food insecurity. Grant eligibility text requires human confirmation beyond the implemented cause/geography match.
- No real labeled financial evaluation, live provider request, real grant, pilot adoption, expert review, outcome improvement, sponsor commitment or governance council is claimed. Optional and licensed integrations remain explicit extension boundaries.
- Production requires dedicated clean storage, strong secrets, exact hosts/HTTPS origins, TLS, encrypted disks/backups, retention/withdrawal replay, configured delivery/monitoring, qualified independent reviewers and human pilot governance. Demo credentials/data must never be promoted.

To resume development: read this checkpoint and AGENTS.md, fetch origin on aarav, keep isolated test databases, make a bounded change and rerun relevant gates. To run the pilot now: `make demo`; use the README's demo-only accounts. No work is promised to continue invisibly after handoff.

## Post-push CI correction

Functional commit 245ff90 was pushed successfully. Its clean Ubuntu workflow passed dependency audits, 192 backend tests, component checks and build, then exposed a queued-import delay behind repeated seed recommendation scans. The trace was inspected; the page remained queued, rather than failing import validation. The follow-up narrows a single-card recommendation event to that card with unchanged recipient consent checks, finishes real seed jobs before browser timing, and retains polling through processing. No timeout/assertion was relaxed. The follow-up `make verify` passed in full: 195 backend tests, 14 component tests, 14 browser journeys, checks/build and real-backend smoke. Three independent worker regressions also passed separately. The corrective commit containing this record is pushed after this gate; the next GitHub run determines its clean-run CI status.

## UI redesign — active 2026-09-19

Scope: presentation and usability only; existing backend, API transport, contracts, data, dependencies, runtime scripts and infrastructure are read-only. `aarav` matched origin at preflight. Untracked `Archive.zip` is unrelated and preserved.

Ownership: coordinator owns application shell, shared primitives, global CSS, layout metadata, integration, redesign docs and all Git operations. Initial native product_architect reconnaissance is read-only across routes and pilot/report shapes. Initial native qa_engineer owns fresh synthetic baseline screenshots and, after explicit handoff, focused UI/browser regression tests; runtime scripts and current substantive journey assertions are read-only. Implementation file assignments follow the baseline and frozen-interface review. No recursive delegation.

Presentation implementation ownership (frozen interfaces): native donor_ui owns only `apps/web/components/donor.tsx`; native nonprofit_ui owns only `apps/web/components/ngo.tsx`. Both preserve request paths/payloads, hook behavior, query semantics, form names/defaults and domain logic. Coordinator retains `ui.tsx`, `application.tsx`, `globals.css`, `layout.tsx` and shared styles. Existing primitives stay source-compatible; Panel gains optional `className` and `variant="open"` for continuous sections. Acceptance includes strict TypeScript/component checks, exact-cent and upload/review browser journeys, before/after inspection. Agents return diff summary, contracts, checks and risks before integration and Git.

Read-only route review completed: 25 workspace route families plus `/admin` alias and public/auth/detail variants. No contract changes needed. Coordinator retains `shared.tsx`, `pilot.tsx` and any report presentation extraction (additional agent thread unavailable); frozen report payloads/permissions/hooks and arbitrary currencies retained. Coordinator owns generic Details fallback and print support in ui.tsx. QA baseline uses existing production `.next` on isolated database at loopback8081; source edits are safe, builds wait until before capture finishes.

Ownership handoff: donor_ui completed donor.tsx and returned it to coordinator; now performs a read-only shell/primitives parity review. nonprofit_ui completed ngo.tsx and returned it to coordinator; now solely owns shared.tsx/pilot.tsx and optional report-content.tsx for the bounded report/shared-screen presentation pass. No overlapping writers. NGO first-pass component run observed 14/14 passing; early TypeScript checks encountered coordinator's not-yet-landed additive UI exports/props, not a baseline application failure.

UI milestone verification: fresh before28/after56 synthetic screenshots captured via original/new production builds on isolated PostgreSQL/API/worker. First visual review prompted smaller evidence metadata, useful allocation rows in review, and denser mobile primary filters. Native read-only product review found no concrete parity blocker; event-handler comparison retained NGO/shared/pilot handlers and form values. Latest `bash scripts/pnpm check`, `bash scripts/pnpm test` (20/20), `bash scripts/pnpm format:check`, and `make build` passed. Browser/print/final integrated gate still pending. All agent writers handed ownership back before staging.

UI report/nonprofit milestone: evidence, uploads, analysis, governance and pilot screens now share the presentation system; reports use known payload shapes plus a complete fallback. Final read-only report review identified the existing three-field manually edited portfolio payload; its context remains visible without an invented budget or empty allocation table. A component regression covers that variant. Strict TypeScript, formatting and 24/24 component tests pass. Initial expanded browser run was 17/24: failures identified a hidden mobile workspace label, enlarged landing-text overflow, and ambiguous new test locators. CSS fixes retain the accessible label and permit long text wrapping; test locators now identify the actual controls/alert. Focused accessibility/print/reflow checks passed 8/8, followed by exact advanced-filter submission/error-retention checks 2/2 on desktop/mobile. An intermediate browser startup overlapped a failed test-type build and is not treated as verification evidence. Final integrated gate and refreshed screenshots follow this checkpoint. All writers remain paused for coordinator-only Git integration.

## UI redesign — final integrated gate

Presentation milestones `ade95e2`, `552a4a6` and `8eeeaea` were committed and pushed to origin/aarav. The final refinement places the selected-organization toolbar above results in normal flow. An unchanged withdrawal assertion exposed a generic alert/Next announcer ambiguity; it now targets the main denial message and additionally asserts “Not found.” Rapid multi-role test setup also hit the existing anonymous throttle; new tests reuse visible sign-in forms and honor an actual bounded `Retry-After` once. Application rate limits, authentication, request timing, polling and all substantive money/persistence/authorization assertions remain unchanged. Failed runs and corrections are recorded in UI_REDESIGN.md.

Final `make verify` after these changes passed, exit0: **195 backend tests in 49.19s, 24 component tests, 24 desktop/mobile browser tests in 1.9m**, checks including API-contract drift, production build and real-backend smoke. Separate frontend check/test/format commands also passed. Accessibility checks cover representative public/auth/discovery/editor/report/review screens with applicable WCAG 2.2 tags; reflow covers 320/390/768/1024/1440 CSS px and 200% text. No whole-app accessibility or scientific certification is implied. Backend, API transport, contracts, fixtures, dependencies, runtime/infrastructure, AGENTS.md and Codex configuration remain unchanged. All subagents completed and returned ownership; only the coordinator performs final Git operations.

Final visual evidence: 28 before and 63 after PNGs under `docs/screenshots/ui-redesign/`, plus capture/inspection JSON. Refreshed all 14 workflow surfaces from the final build; no page/console errors or 1440/390px document overflow observed. Root and independent native visual review opened representative final images; budget/allocation-led reports, normal evidence metadata, explicit invalid-upload state, source details and print expansion remained readable. Supplementary checks cover expanded mobile navigation/focus, active filters and 320px/200% text. The isolated server shut down cleanly, exit0; existing demo/user records were not reset. Ten historical test screenshot files were restored byte-for-byte. The full route checklist, failure/fix history, exact checks, changed paths and remaining coverage limits are in UI_REDESIGN.md. Production prerequisites above remain unchanged. The final evidence/toolbar checkpoint is the commit containing this record; final push status is reported by the coordinating handoff. Untracked `Archive.zip` is preserved.
